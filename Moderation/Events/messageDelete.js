const { MessageEmbed, ClientPresence, MessageActionRow } = require("discord.js")
const set = require("../../settings")
const snipe = require("../Schemas/snipe")

module.exports = async message => {
    if(!message.guild) return;
  //ayarlanacak
}